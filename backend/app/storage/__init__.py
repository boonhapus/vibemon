"""Asset object-store + metadata persistence for Vibemon assets.

Use this package to write/read sprite images, audio, and pose PNGs through
``monstore``, and to upsert their ``AssetRef`` rows via ``assets``.
"""
