interface:
  display_name: "Provider Move Balance"
  short_description: "Audit provider move catalogs against balance gates"
  default_prompt: "Use provider-balance-analysis to audit a provider move catalog (audit_moves.py --provider biome|climate|music)."
