repos:
  - repo: local
    hooks:
      - id: make-quality
        name: make quality
        entry: uv run --project vibemon/backend make.py quality
        language: system
        files: ^
        pass_filenames: false
        stages: [pre-commit]
